// environment.ts — Development settings
// When you run `ng serve`, Angular uses this file.
// When you run `ng build` for production, it swaps in environment.prod.ts automatically.

export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000'   // your local backend
};
