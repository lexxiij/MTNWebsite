// environment.prod.ts — Production settings (used during `ng build`)
// Update apiUrl to your Render backend URL when you deploy.

export const environment = {
  production: true,
  apiUrl: 'https://YOUR-RENDER-BACKEND-URL.onrender.com'
};
